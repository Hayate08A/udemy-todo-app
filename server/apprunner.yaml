version: 1
runtime: nodejs18 # ✅ App Runner の Node.js のバージョン
build:
  commands:
    - cd server && npm install # ✅ 依存関係をインストール
    - npm run build # ✅ TypeScript のビルド
run:
  command: "npm start"
  network:
    port: 3000
